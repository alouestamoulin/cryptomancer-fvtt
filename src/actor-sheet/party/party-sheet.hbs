<form class="crypt-party-sheet" autocomplete="off">
  <nav class="crypt-tabs" data-group="primary">
    <a class="crypt-tab" data-tab="party">{{toUpperCase (l "Party.party")}}</a>
    <a class="crypt-tab" data-tab="safehouse">{{toUpperCase (l "Party.safehouse")}}</a>
  </nav>
  <section class="sheet-body">
    <div class="tab party" data-tab="party">
      <section class="left">
        <div class="avatar">
          <img src="{{actor.img}}"
               title="{{actor.name}}"
               alt="{{actor.name}}"
               data-edit="img"/>
        </div>
        <div class="party-resource">
          <label for="party-risk-{{document.id}}">{{l "Party.risk"}}</label>
          {{> coreInput id=(concat "party-risk-" document.id) value=system.risk.value name="system.risk.value" size="large" color=riskColor}}
        </div>
        <div class="party-resource">
          <label for="party-upgrade-points-{{document.id}}">{{l "Party.upgradePoints"}}</label>
          {{> coreInput id=(concat "party-upgrade-points-" document.id) value=system.upgradePoints name="system.upgradePoints" size="large"}}
        </div>
        <div class="party-resource">
          <label for="party-assets-{{document.id}}">{{l "Party.assets"}}</label>
          {{> coreInput id=(concat "party-assets-" document.id) value=system.assets name="system.assets" size="large"}}
        </div>
        {{#if owner }}
          <button type="button" class="risk-check" data-risk="{{system.risk.value}}">{{l "Party.riskCheck"}}</button>
          <button type="button" class="risk-event-add">{{l "Party.addRiskEvent"}}</button>
          <button type="button" class="cell-add">{{l "Cell.addCell"}}</button>
        {{/if}}
      </section>
      <section class="main">
        <div class="crypt-form-field party-name">
          <label for="party-name-{{document.id}}">{{l "Party.partyName"}}</label>
          <input id="party-name-{{document.id}}"
                 type="text"
                 name="name"
                 value="{{actor.name}}"/>
        </div>
        <h3>{{l "Party.riskEvents"}}</h3>
        {{#each system.riskEvents }}
          <div class="risk-event" data-index="{{@index}}">
            <div class="crypt-toggle-box"
                 data-tooltip="{{l "Party.riskEventCompleted"}}">
              <input class="visually-hidden risk-event-field" id="risk-event-toggle-{{@index}}-{{../document.id}}" type="checkbox" {{checked this.complete}} data-dtype="Boolean"/>
              <label for="risk-event-toggle-{{@index}}-{{../document.id}}"></label>
            </div>
            <input type="text"
                   id="risk-event-text-{{@index}}-{{../document.id}}"
                   class="risk-event-text risk-event-field"
                   value="{{this.eventText}}"/>
            {{#if ../owner }}
              <button type="button" class="risk-event-delete" data-tooltip="{{l 'Delete'}}">
                <span class="fas fa-trash"></span>
              </button>
            {{/if}}
          </div>
        {{/each}}
        <h3>{{l "Party.cells"}}</h3>
        {{#if (is system.cells.length 0) }}
          <p>
            {{l "Party.noCells"}}
          </p>
        {{/if}}
        {{#each system.cells }}
          {{> cell cell=this index=@index documentId=../document.id cellTypes=../cellTypes owner=../owner }}
        {{/each}}
      </section>
    </div>
    <div class="tab safehouse" data-tab="safehouse">
      <div class="safehouse-rooms">
        {{> safehouseRoom room=system.safehouse.cryptovault}}
        {{> safehouseRoom room=system.safehouse.dungeon}}
        {{> safehouseRoom room=system.safehouse.forge}}
        {{> safehouseRoom room=system.safehouse.front}}
        {{> safehouseRoom room=system.safehouse.golem}}
        {{> safehouseRoom room=system.safehouse.laboratory}}
        {{> safehouseRoom room=system.safehouse.lounge}}
        {{> safehouseRoom room=system.safehouse.sanctuary}}
        {{> safehouseRoom room=system.safehouse.stable}}
        {{> safehouseRoom room=system.safehouse.trainingRoom}}
        {{> safehouseRoom room=system.safehouse.warRoom}}
      </div>
    </div>
  </section>
</form>
